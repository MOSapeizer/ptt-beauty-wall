#!/usr/bin/env bash

# echo $GCP_KEY > key.json
# cat key.json
# gcloud auth activate-service-account $GCP_ACCOUNT --key-file key.json
# gcloud --quiet config set project $GCP_PROJECT_ID
# gcloud --quiet app deploy app.yaml --no-promote -v uat

echo "deploy success."